# memory-bench

Use `uv run amb` to run all CLI commands (never activate the venv manually).

Before answering questions about this project — its datasets, memory providers, CLI options, or architecture — load the `/open-memory-benchmark` skill. It is the living documentation for this project and should be kept up to date as things change.
